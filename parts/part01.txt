<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="theme-color" content="#000000" />
  <title>SAFARI 2026 — LEGACY</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Space+Grotesk:wght@500;600;700&display=swap" rel="stylesheet">
  <style>
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

:root {
  --black: #050505;
  --white: #f5f5f2;
  --gray: #9b9b97;
  --line: rgba(255,255,255,.15);
}

html {
  background: var(--black);
  color: var(--white);
  font-family: "Inter", sans-serif;
}

body {
  min-height: 100vh;
  overflow-x: hidden;
  background:
    radial-gradient(circle at 50% 30%, rgba(255,255,255,.055), transparent 35%),
    #050505;
}

button {
  font: inherit;
}

.grain {
  position: fixed;
  inset: 0;
  pointer-events: none;
  z-index: 1000;
  opacity: .08;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 180 180' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='.5'/%3E%3C/svg%3E");
  mix-blend-mode: soft-light;
}

.intro {
  position: fixed;
  inset: 0;
  z-index: 50;
  background: #000;
  display: grid;
  place-items: center;
  overflow: hidden;
  transition: visibility 0s linear 1.3s;
}

.intro::before {
  content: "";
  position: absolute;
  inset: -10%;
  background:
    linear-gradient(90deg, transparent 49.8%, rgba(255,255,255,.08) 50%, transparent 50.2%),
    linear-gradient(0deg, transparent 49.8%, rgba(255,255,255,.05) 50%, transparent 50.2%);
  transform: rotate(-8deg) scale(1.2);
}

.intro-content {
  position: relative;
  z-index: 3;
  text-align: center;
  width: min(92vw, 820px);
}

.eyebrow {
  letter-spacing: .48em;
  font-size: .68rem;
  font-weight: 700;
  color: #b8b8b3;
  margin-bottom: 1.4rem;
  animation: fadeUp .8s ease both;
}

.intro h1 {
  font-family: "Space Grotesk", sans-serif;
  font-size: clamp(3.8rem, 11vw, 8rem);
  line-height: .85;
  letter-spacing: -.07em;
  font-weight: 700;
  animation: fadeUp .9s .08s ease both;
}

.intro h1 span {
  -webkit-text-stroke: 1px var(--white);
  color: transparent;
}

.legacy-wrap {
  display: flex;
  align-items: center;
  gap: 1rem;
  margin: 1.3rem auto 2.8rem;
  width: min(520px, 80vw);
  animation: fadeUp .9s .16s ease both;
}

.legacy-wrap h2 {
  font-size: clamp(1rem, 2.7vw, 1.55rem);
  letter-spacing: .5em;
  font-weight: 500;
  padding-left: .5em;
}

.legacy-line {
  flex: 1;
  height: 1px;
  background: rgba(255,255,255,.55);
}

.enter-btn {
  position: relative;
  z-index: 4;
  width: 180px;
  height: 58px;
  border: 1px solid rgba(255,255,255,.8);
  background: transparent;
  color: white;
  border-radius: 999px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 1.2rem;
  cursor: pointer;
  transition: transform .25s ease, background .25s ease, color .25s ease;
  animation: fadeUp .9s .25s ease both;
  overflow: hidden;
}

.enter-btn::before {
  content: "";
  position: absolute;
  inset: 0;
  background: white;
  transform: translateX(-105%);
  transition: transform .35s cubic-bezier(.2,.8,.2,1);
  z-index: -1;
}

.enter-btn:hover::before {
  transform: translateX(0);
}

.enter-btn:hover {
  color: #000;
  transform: scale(1.03);
}

.enter-btn:active {
  transform: scale(.94);
}

.btn-text {
  font-size: .76rem;
  letter-spacing: .23em;
  font-weight: 800;
}

.btn-arrow {
  font-size: 1.1rem;
}

.intro-note {
  margin-top: 2.2rem;
  font-size: .58rem;
  letter-spacing: .35em;
  color: #6d6d69;
  animation: fadeUp .9s .35s ease both;
}

.corner {
  position: absolute;
  width: 32px;
  height: 32px;
  opacity: .35;
}

.corner-tl { left: 24px; top: 24px; border-left: 1px solid white; border-top: 1px solid white; }
.corner-tr { right: 24px; top: 24px; border-right: 1px solid white; border-top: 1px solid white; }
.corner-bl { left: 24px; bottom: 24px; border-left: 1px solid white; border-bottom: 1px solid white; }
.corner-br { right: 24px; bottom: 24px; border-right: 1px solid white; border-bottom: 1px solid white; }

.transition-orb {
  position: fixed;
  left: 50%;
  top: 50%;
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background: #fff;
  transform: translate(-50%, -50%) scale(0);
  z-index: 20;
  pointer-events: none;
}

.flash {
  position: fixed;
  inset: 0;
  z-index: 21;
  background: white;
  opacity: 0;
  pointer-events: none;
}

.intro.entering .intro-content {
  animation: introCollapse .62s cubic-bezier(.7,0,.3,1) forwards;
}

.intro.entering .transition-orb {
  animation: orbExpand 1.02s cubic-bezier(.76,0,.24,1) .18s forwards;
}

.intro.entering .flash {
  animation: flashBang .72s ease .75s forwards;
}

.intro.finished {
  visibility: hidden;
  pointer-events: none;
}

.dashboard {
  min-height: 100vh;
  opacity: 0;
  transform: scale(1.035);
  filter: blur(12px);
  transition: opacity .8s ease, transform 1s cubic-bezier(.2,.8,.2,1), filter .8s ease;
  padding: 24px;
}

.dashboard.visible {
  opacity: 1;
  transform: scale(1);
  filter: blur(0);
}

.topbar {
  max-width: 1240px;
  margin: 0 auto;
  min-height: 80px;
  border-bottom: 1px solid var(--line);
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.brand {
  display: flex;
  align-items: center;
  gap: 12px;
}

.brand-mark {
  width: 38px;
  height: 38px;
  border: 1px solid var(--white);
  display: grid;
  place-items: center;
  font-family: "Space Grotesk", sans-serif;
  font-weight: 700;
}

.brand strong,
.brand span {
  display: block;
}

.brand strong {
  font-size: .82rem;
  letter-spacing: .08em;
}

.brand span {
  color: var(--gray);
  font-size: .57rem;
  letter-spacing: .2em;
  margin-top: 3px;
}

.status {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: .6rem;
  letter-spacing: .16em;
  color: #b9b9b5;
}

.status-dot {
  width: 7px;
  height: 7px;
  border-radius: 50%;
  background: white;
  box-shadow: 0 0 12px rgba(255,255,255,.8);
  animation: pulse 1.8s infinite;
}

.hero {
  max-width: 1240px;
  margin: 0 auto;
  min-height: 470px;
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  padding: 72px 0 58px;
  border-bottom: 1px solid var(--line);
  gap: 2rem;
}

.section-kicker {
  font-size: .64rem;
  letter-spacing: .32em;
  color: var(--gray);
  margin-bottom: 1.2rem;
}

.hero h3 {
  font-family: "Space Grotesk", sans-serif;
  font-size: clamp(3.2rem, 7vw, 6.2rem);
  line-height: .9;
  letter-spacing: -.055em;
  max-width: 700px;
}

.hero-text {
  margin-top: 2rem;
  color: #a7a7a2;
  max-width: 560px;
  line-height: 1.7;
  font-size: .95rem;
}

.hero-number {
  font-family: "Space Grotesk", sans-serif;
  font-size: clamp(5rem, 15vw, 11rem);
  line-height: .63;
  font-weight: 700;
  letter-spacing: -.12em;
  color: transparent;
  -webkit-text-stroke: 1px rgba(255,255,255,.24);
  text-align: right;
  margin-right: .07em;
}

.hero-number span {
  display: block;
}

.cards {
  max-width: 1240px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1.3fr 1fr 1fr;
  border-left: 1px solid var(--line);
}

.card {
  min-height: 310px;
  padding: 28px;
  border-right: 1px solid var(--line);
  border-bottom: 1px solid var(--line);
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  transition: background .25s ease, color .25s ease;
}

.card:hover {
  background: #f0f0ed;
  color: #050505;
}

.card:hover p,
.card:hover .card-top {
  color: #444;
}

.card-top {
  display: flex;
  justify-content: space-between;
  color: #777;
  font-size: .58rem;
  letter-spacing: .2em;
}

.card h4 {
  font-family: "Space Grotesk", sans-serif;
  font-size: 1.45rem;
  margin-bottom: .75rem;
}

.card p {
  color: #92928d;
  line-height: 1.6;
  font-size: .82rem;
  max-width: 330px;
}

.ghost-btn {
  align-self: flex-start;
  border: 1px solid currentColor;
  background: transparent;
  color: inherit;
  border-radius: 999px;
  padding: 10px 16px;
  font-size: .55rem;
  letter-spacing: .18em;
}

.mini-line {
  height: 1px;
  width: 48px;
  background: currentColor;
  opacity: .5;
}

.footer-strip {
  max-width: 1240px;
  margin: 0 auto;
  min-height: 72px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
  color: #666;
  font-size: .55rem;
  letter-spacing: .18em;
}

@keyframes fadeUp {
  from { opacity: 0; transform: translateY(18px); }
  to { opacity: 1; transform: translateY(0); }
}

@keyframes pulse {
  0%,100% { opacity: .4; }
  50% { opacity: 1; }
}

@keyframes introCollapse {
  0% { opacity: 1; transform: scale(1); filter: blur(0); }
  55% { opacity: 1; transform: scale(.9); filter: blur(1px); }
  100% { opacity: 0; transform: scale(1.18); filter: blur(14px); }
}

@keyframes orbExpand {
  0% {
    transform: translate(-50%, -50%) scale(0);
    box-shadow: 0 0 0 rgba(255,255,255,0);
  }
  45% {
    box-shadow: 0 0 80px rgba(255,255,255,.95);
  }
  100% {
    transform: translate(-50%, -50%) scale(180);
    box-shadow: 0 0 0 rgba(255,255,255,0);
  }
}

@keyframes flashBang {
  0% { opacity: 0; }
  25% { opacity: 1; }
  100% { opacity: 0; }
}

@media (max-width: 860px) {
  .dashboard { padding: 16px; }

  .hero {
    min-height: 520px;
    flex-direction: column;
    align-items: flex-start;
    justify-content: flex-end;
  }

  .hero-number {
    position: absolute;
    right: 22px;
    top: 140px;
    opacity: .6;
  }

  .cards {
    grid-template-columns: 1fr;
  }

  .card {
    min-height: 240px;
  }

  .footer-strip {
    flex-wrap: wrap;
    padding: 20px 0;
  }
}

@media (max-width: 520px) {
  .status { display: none; }

  .intro h1 {
    font-size: clamp(3.5rem, 18vw, 5.3rem);
  }

  .legacy-wrap {
    width: 86vw;
  }

  .hero {
    padding-top: 150px;
  }

  .hero h3 {
    font-size: 3.5rem;
  }

  .hero-number {
    font-size: 6rem;
  }
}

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: .01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: .01ms !important;
  }
}


/* COMODINES FOLDER */
.folder-preview {
  display: flex;
  align-items: center;
  gap: 20px;
}

.folder-icon {
  position: relative;
  width: 72px;
  height: 52px;
  border: 1px solid currentColor;
  border-radius: 3px;
  flex: 0 0 auto;
}

.folder-tab {
  position: absolute;
  left: -1px;
  top: -13px;
  width: 34px;
  height: 13px;
  border: 1px solid currentColor;
  border-bottom: 0;
  border-radius: 3px 3px 0 0;
}

.open-folder-btn {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  cursor: pointer;
  transition: transform .2s ease;
}

.open-folder-btn:hover {
  transform: translateX(4px);
}

.folder-modal {
  position: fixed;
  inset: 0;
  z-index: 900;
  display: grid;
  place-items: center;
  padding: 22px;
  visibility: hidden;
  pointer-events: none;
}

.folder-modal.open {
  visibility: visible;
  pointer-events: auto;
}

.folder-backdrop {
  position: absolute;
  inset: 0;
  background: rgba(0,0,0,.78);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  opacity: 0;
  transition: opacity .35s ease;
}

.folder-modal.open .folder-backdrop {
  opacity: 1;
}

.folder-window {
  position: relative;
  width: min(760px, 100%);
  max-height: min(820px, 88vh);
  overflow: auto;
  background: #f2f2ef;
  color: #050505;
  border: 1px solid rgba(255,255,255,.35);
  box-shadow: 0 30px 100px rgba(0,0,0,.55);
  transform: translateY(22px) scale(.96);
  opacity: 0;
  transition:
    transform .42s cubic-bezier(.2,.8,.2,1),
    opacity .32s ease;
}

.folder-modal.open .folder-window {
  transform: translateY(0) scale(1);
  opacity: 1;
}

.folder-header {
  min-height: 92px;
  padding: 20px 22px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  border-bottom: 1px solid rgba(0,0,0,.17);
}

.folder-title-wrap {
  display: flex;
  align-items: center;
  gap: 15px;
}

.folder-title-wrap p {
  color: #777;
  font-size: .53rem;
  letter-spacing: .23em;
  margin-bottom: 4px;
}

.folder-title-wrap h4 {
  font-family: "Space Grotesk", sans-serif;
  font-size: 1.35rem;
  letter-spacing: .08em;
}

.folder-symbol {
  width: 45px;
  height: 34px;
  border: 1px solid #050505;
  position: relative;
}

.folder-symbol span {
  position: absolute;
  width: 22px;
  height: 9px;
  top: -9px;
  left: -1px;
  border: 1px solid #050505;
  border-bottom: 0;
}

.folder-close {
  width: 42px;
  height: 42px;
  border: 1px solid #050505;
  background: transparent;
  font-size: 1.5rem;
  line-height: 1;
  cursor: pointer;
  transition: background .2s ease, color .2s ease, transform .2s ease;
}

.folder-close:hover {
  background: #050505;
  color: white;
  transform: rotate(90deg);
}

.folder-path {
  padding: 12px 22px;
  border-bottom: 1px solid rgba(0,0,0,.12);
  display: flex;
  gap: 10px;
  font-size: .56rem;
  letter-spacing: .17em;
  color: #81817c;
}

.folder-path strong {
  color: #050505;
}

.comodines-list {
  padding: 0 22px;
}

.comodin-item {
  min-height: 92px;
  display: grid;
  grid-template-columns: 50px 1fr;
  align-items: center;
  border-bottom: 1px solid rgba(0,0,0,.13);
  transition: padding-left .2s ease, background .2s ease;
}

.comodin-item:hover {
  background: #050505;
  color: white;
  padding-left: 12px;
}

.comodin-index {
  font-family: "Space Grotesk", sans-serif;
  font-size: .65rem;
  color: #8b8b86;
}

.comodin-item p {
  font-size: clamp(.95rem, 2vw, 1.15rem);
  line-height: 1.45;
  font-weight: 600;
}

.emoji-row {
  margin-top: 7px;
  font-size: 1.25rem;
  letter-spacing: .08em;
}

.emoji-row.large {
  margin-top: 0;
  font-size: 1.55rem;
}

.folder-footer {
  min-height: 64px;
  padding: 0 22px;
  display: fle